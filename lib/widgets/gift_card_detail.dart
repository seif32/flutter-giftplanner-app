import 'package:flutter/material.dart';

class GiftCardDetail extends StatelessWidget {
  const GiftCardDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('GiftCardDetail');
  }
}
